#include <iostream>
using namespace std;
double func(double x) //объявляем функцию func(x), которая представляет уравнение для поиска корня
{
	return exp(x) + x - 4; //уравнение, для которого ищется корень
}
int main()
{
	setlocale(LC_ALL, "Russian");
	double a, b, e;
	cout << "Введите a: ";
	cin >> a;
	cout << "Введите b: ";
	cin >> b;
	cout << "Введите e: ";
	cin >> e;
	double x; //объявляем текущее приближения корня
	while (abs(a - b) > 2 * e) {
		x = (a + b) / 2; //вычисляем середины интервала выбранного отрезка
		if (func(x) * func(a) <= 0)
			b = x; //если функция имеет разные знаки на концах интервала, то объявляем правую границу
		else
			a = x; //иначе левую
	}
	x = (a + b) / 2; //последнее приближение корня
	cout << "Корень равен: " << x; //вывод искомого корня
}
